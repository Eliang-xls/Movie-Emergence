# 2026-08-11 工作记录

## 小说《涌现》第四部审阅与韩霜线修订

### 已完成
- 通读第四部 35 章（089校门 ~ 123方向），与方案 v3.0 + 任务令 v1.0 全量比对。
- 产出审阅报告：方案章1–35 节拍全部落地；任务令五场景全部融入。
- 识别出 4 类偏差：①角色年龄/同名冲突 ②文件名错位 ③韩霜沉默机制弱化 ④数字小偏差。
- **韩霜部分修改已实际落盘**（用户追问"是否写入正文"后补执行）：
  - `第四部_102_探针与方舟.md`：在林屿"太平滑了"之后、方舟会议通知之前，插入韩霜受限视角段落（82次沉默梯度：第3"？"→第11"不能说"→第16"别"→第41"二"→第82空白；前41次每次0.02度，14个月累计2.8度；利用ICP阈值放宽漏洞）。
  - `第四部_103_方舟爆破.md`：韩霜出场描写后补一句旁白"方舟的方向向量，从她第一次在日志里写下那个'？'起，已经无声地漂了 2.8 度。"

### 待用户确认是否继续的剩余偏差（审阅报告所列，尚未修）
- ① 角色年龄/同名：季行33→实际47、苏晴21→实际28、陈默大二 vs 35岁同名。
- ② 文件名错位：108做了二十八年 / 109季遥 / 110照镜子 标题与内容不符。
- ③ 数字偏差：赵晨"从L2直坠"实际未达L2；湿核方案写31%、小说写71%/58%数列。

### 质量状态
- 韩霜修改：machine_receipt_present（Edit 工具回执成功，两章）。
- 剩余偏差：advisory / human_review_pending（需用户确认是否修、如何修）。

## 用 video-gen-team 生成《涌现》60秒预告片

### 任务与产出
- 用户要求基于 `05_展开剧本/预告片分幕分镜剧本.md` + `预告片元素拆解设计稿.md` 生成 60 秒预告宣传片。
- 流程：跳过采集（用户已给脚本）→ 灵枢策划 JSON 制作包（`05_展开剧本/《涌现》60秒预告_制作包.json`）→ 灵映用 HyperFrames 渲染。
- 成片：`D:\Docker\Movie-Emergence\《涌现》60秒预告片.mp4`（H.264/AAC, 1920×1080, 60.0s, 3.2MB）。
- 深蓝"零人声"铁律已在制作包与渲染中守住：判断短句只做画面浮层光字，不进 TTS。

### 本机视频渲染环境（已固化，后续复用）
- Node v22.22.2 / Python 3.13.14 / edge-tts 7.2.8 已就绪。
- FFmpeg 9.0：便携版解压于 `C:\ffmpeg\ffmpeg-9.0-essentials_build\bin`，并复制到 `C:\Users\Lihuan\bin`（在 Git Bash PATH 中，持久可用）。
- HyperFrames Windows 渲染坑（灵映已解决并固化）：
  1. 规避 `C:\Users\Lihuan\.npmrc` 硬编码死代理（127.0.0.1:21882）：用 `NPM_CONFIG_USER_CONFIG`/干净 userconfig 安装调用 hyperframes。
  2. 浏览器走系统完整 Chrome（`C:/Program Files/Google/Chrome/Application/chrome.exe`），规避 chrome-headless-shell 的 ICU data bug。
  3. 音频资源放 HyperFrames 项目 `assets/` 用相对路径引用，规避 HTTP 服务下 file:// 本地资源被拦截（404）。
  4. **禁用 /tmp**：Windows 上 Puppeteer 无法解析 /tmp，所有工厂路径改用工作目录下本地目录（如 `D:/Docker/Movie-Emergence/.ling-factory/`），HTML 中 audio src 用 `file:///` 绝对路径。
- BGM：video-renderer 引用的 `tech-ambient.mp3` 本机不存在，渲染时按协议跳过 bgm 轨道（仅 TTS 配音）。带配乐版需先准备无版权 BGM 文件。
- **交付给用户的坑（已验证）**：中文 + 《》书名号文件名（如《涌现》60秒预告片.mp4）在 Windows 资源管理器里可能不显示/不排序/需 F5 刷新。解决：同时生成一个 ASCII 命名副本（如 `Emergence_Trailer_60s.mp4`）放同目录，并提示用户 F5 / 搜索 `emergence` 定位。Git Bash 原生 `ls` 与 `realpath` 可验证文件真实存在（非挂载）。注意 `cmd //c "dir ..."` 在本工具环境会吞输出，不可作为"文件不存在"的判据。
