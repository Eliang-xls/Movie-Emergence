# 续接提示词 ·《涌现》正文润色（2026-09-19 定稿）


> ⚠️ **本文件是 2026-09-19 那轮"逐批机械清理"的交接词，已完成，别再照着执行。**
> 现在的工作状态请看 `_本轮总结与待裁定_0920.md`（先跑 `python 体检_全库.py`），并**务必先读 `_禁止改动清单_0920.md`**——里面 30 余条"看着像错其实刻意对"的地方，照旧习惯去"修"会直接改坏正文（头号：`#8901` 与 `#8824` 是两个策元）。新会话请用 `_续接提示词_0920.md`。

> 用途：在**新对话**里继续推进润色时，把本文件全文贴给 AI（或让它先读本文件）。
> 状态：001–181 已完成；**从 182 继续**，剩余 182–199 共 18 章（含 199 终章，需再过一遍）。

---

## 你要帮我做什么

逐批（每批 2 章）把《涌现》正文从**源稿**机械清理后写入**输出目录**。

- 源目录：`D:\Docker\Movie-Emergence\06_小说\小说正文_润色版\`
- 输出目录：`D:\Docker\Movie-Emergence\06_小说\小说正文润色0917\`
- 155–199 是用户本人重写过的（2026-09-19），文笔已到位：**除非人物或逻辑有问题，否则不做内容大改**，只做下面列出的机械清理。

## 每批固定动作（照做，别自创）

1. 把源文件复制进输出目录，做**批量安全替换**：
   - 弯引号 `“ ”` → `「 」`
   - `***`（独占行）→ `---`
   - 删 `**`（加粗标记）、删行首 `# `
   - `极其` / `极度` → 直接删
   - `某种` → `一种`
   - `周岚` / `周老师` → `程岚` / `程老师`（**已裁定全书统一用「程岚」**）
2. 逐处判 `物理` 与 `死死`：
   - `死死` → 一律删（目标 0）。
   - `物理`：**留**——制度术语（物理隔离／切分／交接／管辖／暂停／因果律／做功）＋角色的话（对白／文书／手记／工单／日志）；**删**——叙述里只做强调的（「在物理世界上」「物理含义」「物理后果」「物理常识」「物理距离」「物理气味」等），常换成「在现实里」。
3. 锚点归位：标题下统一加 `二〇XX年·X月｜地点`；原稿没有年月行的章，按前后邻章推断补上，并在交付说明里注明「推断」。
4. 跑终检（应零残留）：`极其|极度|某种|老关|^# |周岚|周老师|十四岁|周敏|林家明|代代言|，的|混泥土|深儿|0Ω|第四颈椎|冰针刺穿前额叶|𝜎`
   - 例外：`*` 若出现在代码里（如 `damping_factor *= 0.42` 的乘号）属豁免，不是 markdown 标记。
5. 更新伴生文档 `_润色进度与逻辑核查_0917.md`（§6.x 逐批追加）＋当日日志 `.workbuddy/memory/YYYY-MM-DD.md`。
6. 每批交付列一张表：字数｜极其｜物理（原→现）｜死死｜引号/星号/井号｜锚点；并用 present_files 展示。

## 重点（务必遵守）

- **182–185 是「PSCAR 两阶段」正典章**（先标定再监听、微带天线、1.4GHz 呼吸、授权演示、魔胎控制流）：**技术描述逐字保留**；数学符号（A₅、σ₁…σ₆₀ 之类）历史上出过乱码，要逐个检查恢复。
- **时间线**：155 之后的「解冻」章是**多线交叉编排**（章节编号向后、时间向前，如 162 是 2048.1、166–169 是 2042.7、170–173 是 2045.12 等）。**先标记、随逐章判定是否合理，不擅自改时间**（这是用户裁定）。
- **人物·策元**：`策元` 不是公司（无总部/编制/雇佣关系）；策元的人出现在外部现场**必须给理由**（协作覆盖区／章程规定的现场复核／跨点支援）。例见 `170` 里韩霜到场的交代。
- **命名**：周岚→程岚（已定）；「周砚」是另一人，保留；「十四岁」已修（林屿当时十九岁，若再出现同类年龄错要修）。
- 字数 <3200 的章，记录进扩写清单。
- 全程用简体中文回答。

## 权威参考（需要时读）

- 叙事口吻标准：`06_小说\00_叙事口吻标准.md`
- 主进度档：`06_小说\小说正文润色0917\_润色进度与逻辑核查_0917.md`（**先读 §6 最新几条，了解进度与待判项**）
- 角色语言风格：`08_设定\04_叙事与视觉设计\00_《涌现》角色人物语言风格设计方案.md`
